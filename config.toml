# Site settings
baseURL = "https://versicolor.io"
languageCode = "en-us"

# Enter your tracking code to enable Google Analytics
googleAnalytics = ""

# Enter your disqus shortname to enable comments
disqusShortname = ""

# Allows us to add raw HTML to a page
[markup]
  [markup.goldmark.renderer]
    unsafe=true
    safeMode='unsafe'

[params]
  # You can use Markdown syntax for urls [text](//url.to/source)
  # To create multi-line text in strings use the """example Text""" format
  #
  # All icons using Fontawesome's icon font. Look at fontawesome.io/icons
  # for more icons. The icons are represented by their corresponding
  # CSS class.

  # Provide your metadata here.
  name = "versicolor"
  description = "An Interactive Journey through statistics, mathematics, physics, and more!"
  favicon = "favicon.ico"

  # 404 error customization
  [params.error404]
    heading = "Page couldn't be found"
    text = "Back to [home](/)"

  # Navigation Section
  [params.navigation]
    title = "Versicolor"
    subtitle = "" # TODO
    menu = "Menu"
    # Optional logo as brand stored in img/
    # logo = "logo.png"

    [[params.navigation.links]]
      name = "Home"
      url = "#"

    # # Add custom links here by uncommenting code below. To remove links just delete
    # # the [[params.navigation.links]] map associated with that link. Copy and paste
    # # code below if you need more links. Buttons work in the same way.
    # #
    # # This fills out the hamburger menu.
    # [[params.navigation.links]]
    #    name = "NameOfLink"
    #    url = "UrlOfLink"
    #
    # # Button Style 1
    # [[params.navigation.button1]]
    #   name = "Get Started"
    #   url = "blogs"
    #
    # # Button Style 2
    # [[params.navigation.button2]]
    #   name = "Log In"
    #   url = "#"

  # Banner section
  [params.banner]
    # To change the background image on the homepage banner, replace 'banner.jpg' in
    # the 'static/img' folder.
    title = "Welcome to Versicolor."
    subtitle = "AN INTERACTIVE JOURNEY THROUGH STATISTICS, MATHEMATICS, PHYSICS, AND MORE"
    buttonText = "Get Started"

  # Tiles Section
  [params.tiles]
    enable = true
    # Display your showcases here.

    [[params.tiles.showcase]]
      title = "Regression"
      subtitle = "Get your knowledge in line"
      image = "menu1.jpg"
      url = "dev/regression"

    [[params.tiles.showcase]]
      title = "Clustering"
      subtitle = ":happy-cluster:"
      image = "menu2.jpg"
      url = "blogs/tempus"

    [[params.tiles.showcase]]
      title = "Time-Series"
      subtitle = "Time keeps on slippin, slippin, slippin"
      image = "menu3.jpg"
      url = "blogs/magna"

    [[params.tiles.showcase]]
      title = "Newtonian Physics"
      subtitle = "Feel the force of good learning"
      image = "menu4.jpg"
      url = "blogs/ipsum"

    [[params.tiles.showcase]]
      title = "Space Stuff"
      subtitle = "I'm out of clever subtitles"
      image = "menu5.jpg"
      url = "blogs/ipsum"

    [[params.tiles.showcase]]
      title = "Quantum Mechanics"
      subtitle = "...or is it?"
      image = "menu6.jpg"
      url = "blogs/ipsum"

  [params.blog]
    # All blogs defined in their own files. You can find example blogs
    # at 'exampleSite/content/blogs'. Copy the 'blogs' folder into the 'content' directory
    # at the root of this Hugo site.
    # For more informtion take a look at the README.
    # To add more blogs just copy and paste the code.

  # Section Type Two
  [params.two]
    enable = false
    title = "Title"
    subtitle = """A callout to some written text. Can be a paragraph or so."""
    buttonText = "Button Text"
    url = "blogs"


  # Contact section
  [params.contact]
    enable = false
    buttonSend = "Send message"
    buttonClear = "Clear"

    # Since this template is static, the contact form uses www.formspree.io as a
    # proxy. The form makes a POST request to their servers to send the actual
    # email. Visitors can send up to a 1000 emails each month for free.
    #
    # What you need to do for the setup?
    #
    # - set your email address under 'email' below and uncomment
    # - upload the generated site to your server
    # - send a dummy email yourself to confirm your account
    # - click the confirm link in the email from www.formspree.io
    # - you're done. Happy mailing!
    # formEmail = "example@domain.com"

    # Contact Section
    # You can add multiple emails, phone numbers, or addresses
    # by copying the corresponding map with frontmatter code.
    [[params.contact.email]]
    title = "Email"
    email = "example@domain.com"

    [[params.contact.phone]]
    title = "Phone"
    phone = "(000) 000-0000 x1234"

    [[params.contact.address]]
    title = "Address"
    street = "Street"
    city = "City, ST 00000"
    country = "Country"

  # Footer section
  [params.footer]
    enable = true
    copyright = "Versicolor"

    # Uses font awesome icons
    [[params.footer.social]]
      icon = "fa-github"
      url = "https://www.github.com/versicolor"
      label = "GitHub"

    # Socials don't render with any text by default, so having a line of person
    # socials gets confusing.
    #
    # TODO: Figure out if we want socials and add better labels, or delete this
    # comment block.
    #
    # [[params.footer.social]]
    #   icon = "fa-twitter"
    #   url = "https://twitter.com/beccalunch"
    #   label = "Becca"

    # [[params.footer.social]]
    #   icon = "fa-twitter"
    #   url = "https://www.twitter.com/StefanoWithAnF"
    #   label = "Stefano"

    # [[params.footer.social]]
    #   icon = "fa-linkedin"
    #   url = "https://www.linkedin.com/in/becca-lynch/"
    #   label = "LinkedIn (Becca)"

    [[params.footer.quicklinks]]
      url = "https://beccalynch.com"
      alt = "Becca Lynch"
